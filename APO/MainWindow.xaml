<Window x:Class="APO.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="APO - Oskar Jabłoński" Height="100" Width="350">

    <Grid>
        <Menu>
            <MenuItem Header="File">
                <MenuItem x:Name="OpenImageMenuItem" Header="Open Image..." Click="OpenImageMenuItem_Click" />
            </MenuItem>
            <MenuItem Header="Info" x:Name="info" Click="info_Click"/>
            <MenuItem x:Name="Operations" Header="Operations">
                <MenuItem x:Name="Math" Header="Math">
                    <MenuItem x:Name="Blend" Header="Blend" Click="Blend_Click"/>
                    <MenuItem x:Name="BitwiseOperations" Header="Bitwise Operations">
                        <MenuItem x:Name="AND" Header="AND" Click="AND_Click"/>
                        <MenuItem x:Name="OR" Header="OR" Click="OR_Click"/>
                        <MenuItem x:Name="XOR" Header="XOR" Click="XOR_Click"/>
                    </MenuItem>
                </MenuItem>
            </MenuItem>
        </Menu>
    </Grid>
</Window>
